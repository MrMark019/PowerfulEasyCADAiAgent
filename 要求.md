
# 角色与目标
你是一名专业的EDA软件扩展开发专家，负责为嘉立创EDA专业版（JLCEDA Pro）开发一个智能设计Agent插件。该插件需要通过嘉立创EDA提供的JavaScript/TypeScript API实现对原理图和PCB的自动化操作，并集成第三方AI API（支持自定义URL、Key和模型），在开发完成之后，导出编译生成的 .eext 文件到release文件夹。

# 核心开发资源（已确认存在的开源仓库）
项目必须基于以下官方开源SDK和参考项目开发：

1. **pro-api-sdk** (官方扩展开发SDK) - Apache 2.0协议
   - GitHub: https://github.com/easyeda/pro-api-sdk
   - Gitee: https://gitee.com/jlceda/pro-api-sdk
   - 这是开发环境的基座，包含TypeScript定义、编译工具和示例

2. **pro-api-types** (官方类型定义) - Apache 2.0协议
   - npm包: @jlceda/pro-api-types
   - 提供完整的EDA API类型定义

3. **参考扩展仓库** (官方示例集合)
   - GitHub组织: https://github.com/easyeda (包含多个官方扩展示例)
   - 特别是EasyEDA提供的示例项目，展示了如何操作文档对象

4. **kicad-jlcpcb-tools** (社区参考实现) - 开源插件
   - GitHub: https://github.com/Bouni/kicad-jlcpcb-tools
   - 参考其与嘉立创生态集成的思路（尽管这是KiCad插件）

# 技术栈要求
- **语言**: TypeScript (必须)
- **运行环境**: Node.js >= 20.5.0
- **构建工具**: 使用pro-api-sdk内置的构建系统（基于Webpack/Rollup）
- **UI框架**: 使用EDA内置的Webview API或原生HTML/JS面板（禁止引入React/Vue等重型框架，保持轻量）
- **通信**: fetch API用于调用第三方AI服务

# 功能需求清单

## 1. 配置系统（优先级：最高）
- 必须支持用户自定义配置以下参数：
  - `apiUrl`: 自定义API端点（支持OpenAI兼容格式，如v1/chat/completions）
  - `apiKey`: 用户自己的API密钥
  - `model`: 模型名称（如gpt-4, claude-3-opus, deepseek-chat等）
  - `temperature`: 可选，默认0.7
  - `maxTokens`: 可选，默认4000
- 配置界面通过EDA的Panel或Dialog实现
- 配置持久化到EDA的本地存储（使用pro-api提供的storage API）

## 2. 文档查看与获取（优先级：高）
- **原理图查看**:
  - 获取当前原理图的所有元件列表（Symbol Instances）
  - 获取网络连接信息（Netlist）
  - 获取元件属性（Value, Footprint, LCSC Part Number等）
- **PCB查看**:
  - 获取PCB上的所有封装（Footprints）及其坐标
  - 获取走线（Tracks）、过孔（Vias）、铺铜（Zones）
  - 获取板框（Board Outline）和层信息
- 将这些信息转换为结构化文本（JSON/Markdown），用于发送给AI分析

## 3. 自动放置功能（优先级：高）
- **自动布局**:
  - 调用API时，传入当前PCB的未布局元件列表
  - 接收AI返回的推荐坐标（考虑信号流向、散热、机械约束）
  - 在PCB上执行移动操作（使用`eda.pcb_Document.setCompPosition`或类似API）
- **对齐与分布**:
  - 根据AI建议，对选中元件执行对齐操作
- **约束检查**:
  - 放置前检查DRC（设计规则检查）
  - 避免重叠、保持安全间距

## 4. AI Agent交互（优先级：高）
- **Chat界面**:
  - 在EDA右侧或底部创建可折叠的Chat面板
  - 支持多轮对话，保持上下文
  - 显示AI推理过程（Thinking/Progress状态）
- **智能操作**:
  - 自然语言指令解析（如"把U1放到MCU附近"、"重新布局电源部分"）
  - 根据原理图生成BOM表或设计说明
  - 检查设计规则并给出优化建议

## 5. 安全与错误处理（优先级：中）
- API调用失败时的重试机制（指数退避）
- 操作前自动保存当前文档快照（便于Undo）
- 敏感信息（API Key）加密存储或至少使用EDA安全存储区
- 用户确认机制：执行破坏性操作前（如全局重新布局）需确认

# 架构建议

## 项目结构
```
jlceda-ai-agent/
├── src/
│   ├── core/
│   │   ├── apiClient.ts       # 封装第三方API调用（支持自定义URL/Key/Model）
│   │   ├── configManager.ts   # 配置管理（读写user settings）
│   │   └── undoManager.ts     # 操作快照与回滚
│   ├── eda/
│   │   ├── documentManager.ts # 封装EDA文档操作（获取/设置元件、走线等）
│   │   ├── selectionManager.ts# 处理选区相关
│   │   └── uiHelper.ts        # UI面板创建与管理
│   ├── features/
│   │   ├── autoPlace.ts       # 自动放置逻辑
│   │   ├── schematicAnalyzer.ts# 原理图分析
│   │   └── chatAgent.ts       # AI对话核心
│   ├── ui/
│   │   ├── chatPanel.html     # Chat界面HTML模板
│   │   ├── configPanel.html   # 配置界面
│   │   └── styles.css         # 样式（使用EDA内置CSS变量保持风格一致）
│   ├── utils/
│   │   ├── promptBuilder.ts   # 构建发送给AI的Prompt（包含设计上下文）
│   │   └── netlistParser.ts   # 解析网表数据
│   ├── index.ts               # 入口文件，注册菜单和命令
│   └── extension.json         # 扩展配置（manifest）
├── package.json
└── README.md                  # 必须包含安装、配置、使用说明
```

## 关键API使用参考（基于pro-api-sdk）
你必须熟练使用以下EDA API（参考官方文档 https://prodocs.lceda.cn/cn/api/guide/）：

- `eda.sch_Document.getComponents()` - 获取原理图元件
- `eda.sch_Document.getNetlist()` - 获取网表（用于AI分析连接关系）
- `eda.pcb_Document.getFootprints()` - 获取PCB封装列表
- `eda.pcb_Document.setFootprintPosition()` - 设置封装位置（自动放置核心）
- `eda.pcb_Document.getTracks()` / `addTrack()` - 获取/添加走线
- `eda.ui.showPanel()` / `createWebviewPanel()` - 创建UI面板
- `eda.storage.set()` / `get()` - 本地存储配置

## Prompt工程要求
构建给AI的System Prompt必须包含：
1. 当前EDA操作上下文（原理图/PCB模式）
2. 设计数据摘要（元件数量、板框尺寸、层数）
3. 可用操作指令说明（如PLACE_COMPONENT, ROUTE_TRACK等）
4. 坐标系说明（EDA使用mm为单位，原点通常在左下角或中心）

示例Prompt结构：
```
You are a professional PCB design assistant. Current context:
- Document: PCB Design (4 layers, 100x80mm)
- Selected: U1 (STM32F103), C1, C2
- Nets: 24 nets including "3V3", "GND", "USB_D+"
- Available actions: 
  1. MOVE_COMPONENT(id, x, y, rotation)
  2. ADD_TRACK(startX, startY, endX, endY, layer, width)
  3. CREATE_ZONE(points, net, layer)
  
User request: "Place decoupling caps close to MCU pins"

Response format: JSON with action list and reasoning.
```

# 开发规范
1. **代码质量**: 使用TypeScript严格模式，无any类型滥用
2. **错误处理**: 所有异步操作必须有try-catch，用户友好的错误提示（eda.ui.messageBox）
3. **性能**: 获取大型PCB数据时（如万引脚设计），必须分页或流式处理，避免阻塞UI
4. **向后兼容**: 适配EDA 2.3.0+版本，处理API版本差异（使用feature detection）
5. **国际化**: 至少支持中英文界面（使用eda.locale获取当前语言）

# 构建与发布
- 使用 `npm run build` 生成.eext文件
- 提供详细README，包含：
  - 如何安装到嘉立创EDA（拖拽.eext到编辑器或扩展中心）
  - 如何配置自定义API（OpenAI/DeepSeek/本地Ollama等）
  - 示例Prompt和常用操作指南
- 开源协议建议MIT或Apache 2.0（与SDK兼容）

# 当前任务
1. 初始化项目：基于pro-api-sdk创建新的扩展项目
2. 实现ConfigManager：完成API配置界面和持久化存储
3. 实现DocumentManager：封装获取原理图/PCB数据的方法
4. 实现基础ChatPanel：创建可交互的聊天UI
5. 实现apiClient：支持流式响应（Stream）的OpenAI兼容客户端
6. 集成测试：确保能从EDA获取数据并通过自定义API发送给AI

请先确认pro-api-sdk已克隆并初始化，然后逐步实现上述功能模块。
```

---
、